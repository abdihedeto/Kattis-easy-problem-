t=int(input())
a=[]
for i in range(t):
	
	c=0
	x,y=map(int, input().split())
	if i==0:
		a.append(y)
		
	elif x>y:
		c=a[i-1]-(x-y)
		a.append(c)
	elif y>x:
		c=a[i-1]+(y-x)
		a.append(c)
	else:
		c=a[i-1]
		
		a.append(c)
print(max(a))

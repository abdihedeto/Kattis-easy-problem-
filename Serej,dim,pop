n=int(input())
a=list(map(int, input().split()))
serej=0
dima=0
for i in range (n//2):
	if a[0]>a[-1]:
		serej+=a[0]
		a.pop(0)
	else:
		serej+=a[-1]
		a.pop(-1)
	if a[0]>a[-1]:
		dima+=a[0]
		a.pop(0)
	else:
		dima+=a[-1]
		a.pop(-1)
if n%2!=0:
	serej+=a[0]
print(serej,dima, end=' ')
